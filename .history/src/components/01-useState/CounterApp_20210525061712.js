import React from 'react'
import './counter.css'

export const CounterApp = () => {

    const [counter, setounter] = useState(initialState)
counter] = useState(initialState)

    return (
        <div>
            
        </div>
    )
}
