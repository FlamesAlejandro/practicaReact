import React from 'react'

export const TodoList = (todos, ) => {
    return (
        <ul className="list-group list-group-flush">
            {
                todos.map( (todos, i) => (
                    
                ))
            }
        </ul>
    )
}
