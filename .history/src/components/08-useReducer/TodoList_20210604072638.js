import React from 'react'

export const TodoList = () => {
    return (
        <ul className="list-group list-group-flush">
            {
                todos.map( (todos, i) => (
                    
                ))
            }
        </ul>
    )
}
