import React from 'react'

export const TodoList = (todos, index, handleDelete, ) => {
    return (
        <ul className="list-group list-group-flush">
            {
                todos.map( (todos, i) => (
                    
                ))
            }
        </ul>
    )
}
