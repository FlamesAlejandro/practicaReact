import {React, useContext} from 'react'
import { UserContext } from './UserContext'

export const HomeScreen = () => {
    
    const useContext = useContext(contextValue);

    return (
        <div>
            <h1>HomeScreen</h1>
            <hr />
            
        </div>
    )
}
